dubbo:
  application:
    name: dubbo-rest-demo-consumer
  protocol:
    name: rest
    port: -1
    serialization: fastjson2
  registry:
    address: zookeeper://${zookeeper.address:127.0.0.1}:2180

server:
  port: 8083